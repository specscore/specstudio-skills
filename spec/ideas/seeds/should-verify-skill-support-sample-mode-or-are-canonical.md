---
captured_by: user
status: queued
---

# should verify skill support a --sample N mode (verify a subset of ACs) or are canonical full runs the only contract — surfaced during dogfood when ad-hoc sampling forced filename and event-payload extensions

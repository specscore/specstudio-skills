```yaml
feature: approval-autonomy
revision: 2961c6c
verdicts:
  - ac: approval-autonomy#ac:fires-pre-commit-and-pre-push
    verdict: pass
    justification: "Step 13 fires implementation.pre_commit before each commit; step 14 fires implementation.pre_push before promote; each proceeds only on Approved, blocked with findings on Issues Found."
  - ac: approval-autonomy#ac:no-hardcoded-approval
    verdict: pass
    justification: "Step 13: noop/deterministic-only gate releases autonomously (no human prompt); a type:human reviewer stops for that human before committing. No hardcoded per-batch approval step."
  - ac: approval-autonomy#ac:cadence-default-and-config
    verdict: pass
    justification: "commit_cadence table: batch is default when unset; task at project scope with no narrower override resolves to one commit per task, across the run/session/project/user ladder."
  - ac: approval-autonomy#ac:pre-commit-fires-per-commit
    verdict: pass
    justification: "Cadence-drives-firing: a batch run producing three commits fires implementation.pre_commit three times, once per commit, each an independent gate evaluation (runner per-occurrence + events multi-fire)."
  - ac: approval-autonomy#ac:halts-on-each-anomaly
    verdict: pass
    justification: "Anomaly Halts: regardless of gate config (including noop) the run MUST halt on sibling conflict / BLOCKED subagent / unresolved-after-fix lint / source-Feature drift; not subject to the gate verdict."
  - ac: approval-autonomy#ac:halt-surfaces-cause-no-autoresolve
    verdict: pass
    justification: "On halt the skill MUST stop, name the specific cause (table maps each anomaly to its surfaced cause), perform no auto-resolution, and not advance."
  - ac: approval-autonomy#ac:re-arm-required
    verdict: pass
    justification: "Explicit re-arm: MUST NOT auto-resume when the anomaly clears; resumes only after the user issues the explicit lowercase token `continue`."
  - ac: approval-autonomy#ac:re-arm-scoped-to-run
    verdict: pass
    justification: "Re-arm re-enables autonomy for the current run only; a subsequent run starts from the resolved autonomy:/gates: config, never from a prior run's re-armed state."
  - ac: approval-autonomy#ac:cumulative-review-presented
    verdict: pass
    justification: "Cumulative Review section: a type:human pre_push reviewer is shown the full set of run commits (consolidated diff, or per-commit summary beyond ~150 lines / ~10 files), not just the final commit."
  - ac: approval-autonomy#ac:push-still-branch-safe
    verdict: pass
    justification: "Push Safety Floor: every push routes through publication-policy branch-safety; a denied branch is refused regardless of autonomy:/gates: settings; autonomy cannot bypass it."
  - ac: approval-autonomy#ac:config-under-autonomy-namespace
    verdict: pass
    justification: "Autonomy knobs live under top-level autonomy: keyed by skill (autonomy.implement.commit_cadence); workflow-step names (e.g. implement:) MUST NOT appear as top-level config keys."
  - ac: approval-autonomy#ac:branch-mask-as-gate-when
    verdict: pass
    justification: "runner Step 1.5 + walk-through: on a feature branch a when:-masked human entry does not participate (commits autonomous); on main it participates (human asked) — behavior entirely from the gate-entry when: condition."
  - ac: approval-autonomy#ac:current-branch-reconciled
    verdict: pass
    justification: "REQ protected-branch-commit-confirmation no longer requires commit-time confirmation (checkpoint moved to promote/push); specscore spec lint reports 0 violations."
```

# Verify Report — approval-autonomy @ 2961c6c

13 ACs verified; **13 passed, 0 failed, 0 unmapped, 0 errored**. Each AC was judged by an independent verifier subagent (serial dispatch) against the `Verifies:`-trailed commits. This Feature is implemented as instructional contract Markdown (the `implement` skill + the shared `reviewer-gates` loader/runner + affected-Feature revisions); verdicts assess whether the committed contract text satisfies each AC's `Then` clause.

## AC: fires-pre-commit-and-pre-push

**Verdict:** pass

**Justification:** Step 13 fires `implementation.pre_commit` before each commit (multi-fire, blocks on `Issues Found` with findings surfaced); step 14 fires `implementation.pre_push` before promote, push proceeding only on `Approved` and blocked with findings on `Issues Found`.

**Commits:**
- 8acb3a1 — feat(approval-autonomy): gate-config-driven commit/push in implement (Task 3)

**Evidence:**
- skills/implement/SKILL.md (steps 12–14, Hard Gate items 4–5, Red Flags)
- skills/shared/events.md (pre_commit before each commit / pre_push before promote, multi-fire)
- skills/shared/reviewer-gates/loader.md, runner.md (Approved vs Issues Found evaluation)

## AC: no-hardcoded-approval

**Verdict:** pass

**Justification:** Step 13 mandates a `noop`/`deterministic`-only `implementation.pre_commit` gate releases autonomously with no human prompt, while a `type: human` reviewer stops for that human before committing — no hardcoded per-batch approval step.

**Commits:**
- 8acb3a1 — feat(approval-autonomy): gate-config-driven commit/push in implement (Task 3)

**Evidence:**
- skills/implement/SKILL.md (Hard Gate item 4, Checklist step 13)
- Red Flags forbid an unconditional approval step and prompting a human when the gate is `noop`/`deterministic`-only

## AC: cadence-default-and-config

**Verdict:** pass

**Justification:** The `commit_cadence` table sets `batch` as default when unset; resolution prose: "When unset at every scope, the cadence is `batch`" and "`commit_cadence: task` at project scope with no narrower override resolves to `task` → one commit per task."

**Commits:**
- 1fc0347 — feat(approval-autonomy): cadence, anomaly-halts, re-arm, cumulative review, push floor (Tasks 4-8)

**Evidence:**
- skills/implement/SKILL.md ("Commit Cadence and the `autonomy:` Namespace" — table + resolution paragraph)

## AC: pre-commit-fires-per-commit

**Verdict:** pass

**Justification:** "Cadence drives `pre_commit` firing": a batch-cadence run with three batch commits fires `implementation.pre_commit` three times, once before each commit, each an independent gate evaluation — backed by runner per-occurrence semantics and the events multi-fire contract.

**Commits:**
- 1fc0347 — feat(approval-autonomy): cadence, anomaly-halts, re-arm, cumulative review, push floor (Tasks 4-8)

**Evidence:**
- skills/implement/SKILL.md ("### Cadence drives `pre_commit` firing"; Checklist step 13)
- skills/shared/events.md (multi-fire, no single-shot caching)
- skills/shared/reviewer-gates/runner.md (Step 7 per-occurrence + three-occurrence harness)

## AC: halts-on-each-anomaly

**Verdict:** pass

**Justification:** "Regardless of gate configuration — including a fully autonomous (`noop`) `pre_commit` gate — `implement` MUST halt the run on any of: (a) sibling merge conflict; (b) BLOCKED subagent; (c) unresolved-after-`--fix` lint; (d) source-Feature drift ... not subject to the gate verdict."

**Commits:**
- 1fc0347 — feat(approval-autonomy): cadence, anomaly-halts, re-arm, cumulative review, push floor (Tasks 4-8)

**Evidence:**
- skills/implement/SKILL.md ("Anomaly Halts and Re-arm" section)

## AC: halt-surfaces-cause-no-autoresolve

**Verdict:** pass

**Justification:** "On an anomaly-halt, `implement` MUST stop, name the specific cause, perform no auto-resolution, and not advance" + table mapping each of the four anomalies to its specific surfaced cause.

**Commits:**
- 1fc0347 — feat(approval-autonomy): cadence, anomaly-halts, re-arm, cumulative review, push floor (Tasks 4-8)

**Evidence:**
- skills/implement/SKILL.md ("Anomaly Halts and Re-arm" cause-surfacing table; "not subject to the gate verdict")

## AC: re-arm-required

**Verdict:** pass

**Justification:** "After the user addresses the cause, `implement` MUST NOT auto-resume when the anomaly clears. It resumes autonomous execution only after the user issues an explicit re-arm signal — the lowercase standalone token `continue`."

**Commits:**
- 1fc0347 — feat(approval-autonomy): cadence, anomaly-halts, re-arm, cumulative review, push floor (Tasks 4-8)

**Evidence:**
- skills/implement/SKILL.md ("Explicit re-arm" subsection; Checklist + Red Flags reinforce no auto-resume without `continue`)

## AC: re-arm-scoped-to-run

**Verdict:** pass

**Justification:** "A re-arm re-enables autonomy for the remainder of the current `implement` run only. A subsequent run starts from the resolved `autonomy:` / `gates:` config — never from a prior run's re-armed state."

**Commits:**
- 1fc0347 — feat(approval-autonomy): cadence, anomaly-halts, re-arm, cumulative review, push floor (Tasks 4-8)

**Evidence:**
- skills/implement/SKILL.md ("Explicit re-arm" scope statement; Checklist "scoped to the current run"; Red Flag against carrying re-armed state into a later run)

## AC: cumulative-review-presented

**Verdict:** pass

**Justification:** "MUST present a cumulative review ... the full set of commits accumulated during the run, not merely the final commit" — consolidated diff for small changes, per-commit summary beyond ~150 changed lines OR ~10 files.

**Commits:**
- 1fc0347 — feat(approval-autonomy): cadence, anomaly-halts, re-arm, cumulative review, push floor (Tasks 4-8)

**Evidence:**
- skills/implement/SKILL.md ("Cumulative Review at the Push Gate" section; Checklist step 14)

## AC: push-still-branch-safe

**Verdict:** pass

**Justification:** "Push Safety Floor": every promote/push routes through publication-policy push branch-safety; autonomy MUST NOT weaken or bypass it — a publication-policy-denied branch is refused regardless of `autonomy:`/`gates:` settings.

**Commits:**
- 1fc0347 — feat(approval-autonomy): cadence, anomaly-halts, re-arm, cumulative review, push floor (Tasks 4-8)

**Evidence:**
- skills/implement/SKILL.md ("## Push Safety Floor"; Checklist step 14; "the `pre_push` gate complements it, does not replace it")

## AC: config-under-autonomy-namespace

**Verdict:** pass

**Justification:** Autonomy knobs live under a top-level `autonomy:` key keyed by skill name (`autonomy.implement.commit_cadence`); workflow-step names (e.g., `implement:`) MUST NOT appear as top-level config keys.

**Commits:**
- 1fc0347 — feat(approval-autonomy): cadence, anomaly-halts, re-arm, cumulative review, push floor (Tasks 4-8)

**Evidence:**
- skills/implement/SKILL.md ("Commit Cadence and the `autonomy:` Namespace" intro; Checklist + Red Flags)

## AC: branch-mask-as-gate-when

**Verdict:** pass

**Justification:** runner Step 1.5 + dedicated walk-through: on a feature branch a `when:`-masked human entry does not participate (neither dispatched nor counted; commits stay autonomous); on `main` it matches and the human is asked — behavior coming entirely from the gate-entry `when:` condition.

**Commits:**
- 66a3b19 — feat(approval-autonomy): when: branch masks + current-branch Option B (Tasks 1-2)

**Evidence:**
- skills/shared/reviewer-gates/runner.md (Step 1.5 + walk-through)
- skills/shared/reviewer-gates/loader.md (Step 3-when validation; `when` on normalized record)
- spec/features/reviewer-gates/README.md (REQ gate-entry-when-condition; AC when-condition-masks-by-branch)

## AC: current-branch-reconciled

**Verdict:** pass

**Justification:** REQ `protected-branch-commit-confirmation` now allows a current-branch run to commit onto a protected branch WITHOUT a commit-time confirmation (checkpoint relocated to promote/push); `specscore spec lint` reports 0 violations.

**Commits:**
- 66a3b19 — feat(approval-autonomy): when: branch masks + current-branch Option B (Tasks 1-2)

**Evidence:**
- spec/features/implement-execution-topology/current-branch/README.md (revised REQ + AC protected-branch-committed-without-commit-time-prompt)
- `specscore spec lint` → 0 violations

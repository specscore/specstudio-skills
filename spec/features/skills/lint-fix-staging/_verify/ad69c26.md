```yaml
feature: skills/lint-fix-staging
revision: ad69c26
verdicts:
  - ac: skills/lint-fix-staging#ac:reads-report-not-git-diff
    verdict: pass
    justification: "Protocol step 1 and rule reads-report-not-git-diff mandate deriving the set from --format json .fixed[] and forbid parsing git status/diff"
  - ac: skills/lint-fix-staging#ac:stages-every-reported-path
    verdict: pass
    justification: "Step 3 and rule stages-every-reported-path require git add of the union (artifacts + every .fixed[] path), none dropped as unrelated"
  - ac: skills/lint-fix-staging#ac:surfaces-staged-lint-fixes
    verdict: pass
    justification: "Step 4 and rule surfaces-staged-lint-fixes require listing lint-induced paths explicitly and distinctly from the skill's own artifacts"
  - ac: skills/lint-fix-staging#ac:stages-never-commits
    verdict: pass
    justification: "Step 5 and rule stages-never-commits forbid git commit/push; only the index is updated"
  - ac: skills/lint-fix-staging#ac:blocks-when-report-missing
    verdict: pass
    justification: "Step 2 degraded-mode guard and rule blocks-when-report-missing require surfacing the limitation and forbid git-diff reconstruction"
```

# Verify Report — skills/lint-fix-staging @ ad69c26

Per-AC verdicts for the lint-fix-staging Feature at revision `ad69c26`. The deliverable is the shared contract document `skills/shared/lint-fix-staging.md`; each AC is satisfied where the contract states the rule clearly enough that a skill following it exhibits the Then behavior. (Per-skill wiring is deferred per the Feature's Open Questions.)

**Tally:** 5 passed, 0 failed, 0 unmapped, 0 errored (5 total).

## AC: reads-report-not-git-diff

**Verdict:** pass

**Justification:** Protocol step 1 and the `reads-report-not-git-diff` rule mandate deriving the modified-file set from the CLI's `--format json` `.fixed[]` array and forbid parsing `git status`/`git diff`.

**Commits:**
- ad69c26 — feat(skills): add shared lint-fix-staging contract

**Evidence:**
- `skills/shared/lint-fix-staging.md` — Protocol step 1; Rules: `reads-report-not-git-diff`

## AC: stages-every-reported-path

**Verdict:** pass

**Justification:** Protocol step 3 ("Stage the union") and the `stages-every-reported-path` rule require `git add` of the union of the skill's own artifacts and every `.fixed[]` path, explicitly forbidding dropping a reported index README / lifecycle sync as "unrelated".

**Commits:**
- ad69c26 — feat(skills): add shared lint-fix-staging contract

**Evidence:**
- `skills/shared/lint-fix-staging.md` — Protocol step 3; Rules: `stages-every-reported-path`

## AC: surfaces-staged-lint-fixes

**Verdict:** pass

**Justification:** Protocol step 4 and the `surfaces-staged-lint-fixes` rule require listing the lint-induced paths explicitly and distinctly from the skill's own artifacts, so nothing is staged silently.

**Commits:**
- ad69c26 — feat(skills): add shared lint-fix-staging contract

**Evidence:**
- `skills/shared/lint-fix-staging.md` — Protocol step 4; Rules: `surfaces-staged-lint-fixes`

## AC: stages-never-commits

**Verdict:** pass

**Justification:** Protocol step 5 ("Never commit") and the `stages-never-commits` rule require the skill not to run `git commit`/`git push`; only the git index is updated.

**Commits:**
- ad69c26 — feat(skills): add shared lint-fix-staging contract

**Evidence:**
- `skills/shared/lint-fix-staging.md` — Protocol step 5; Rules: `stages-never-commits`

## AC: blocks-when-report-missing

**Verdict:** pass

**Justification:** Protocol step 2 (degraded-mode guard) and the `blocks-when-report-missing` rule require surfacing the missing-capability limitation and forbid reconstructing the change set from a `git status`/`git diff` heuristic.

**Commits:**
- ad69c26 — feat(skills): add shared lint-fix-staging contract

**Evidence:**
- `skills/shared/lint-fix-staging.md` — Protocol step 2; Rules: `blocks-when-report-missing`

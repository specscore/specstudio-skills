```yaml
feature: skills/ship
revision: 2381566
verdicts:
  - ac: skills/ship#ac:rejects-non-feature-input
    verdict: pass
    justification: "Pre-Flight step 1 requires zero/>1 args to print usage, write nothing, exit non-zero."
  - ac: skills/ship#ac:refuses-non-implementing-status
    verdict: pass
    justification: "Pre-Flight step 2 + preamble: print current Status, refuse, dispatch no reviewer/delegate, exit non-zero."
  - ac: skills/ship#ac:refuses-when-verify-not-green
    verdict: pass
    justification: "Pre-Flight step 3 refuses unless every verify AC is pass (or report missing), names condition, recommends verify, exits non-zero."
  - ac: skills/ship#ac:refuses-on-recap-contradiction
    verdict: pass
    justification: "Pre-Flight step 4 refuses on any contradiction or missing recap, names condition, recommends recap, exits non-zero."
  - ac: skills/ship#ac:gate-releases-only-on-all-approved
    verdict: pass
    justification: "Reviewer Gate releases only on every-Approved; Issues Found halts with no delegate; ship.pre_dispatch registered in events.md + reviewer-gates."
  - ac: skills/ship#ac:dispatches-single-configured-delegate
    verdict: pass
    justification: "Deploy Dispatch item 1 invokes the single configured delegate exactly once with args, no sequencing/retry; ship: schema present."
  - ac: skills/ship#ac:hands-back-when-no-delegate
    verdict: pass
    justification: "Deploy Dispatch item 2: with no delegate, summarize, state none configured, transition no status, emit no ship.completed, exit without deploy."
  - ac: skills/ship#ac:no-transition-on-delegate-failure
    verdict: pass
    justification: "Deploy Dispatch item 3: on failure/ambiguous, leave Implementing, no retry, surface outcome, emit no ship.completed."
  - ac: skills/ship#ac:transitions-to-stable-on-success
    verdict: pass
    justification: "Lifecycle Transition step 1 runs `specscore feature change-status <slug> --to Stable` only on explicit delegate success."
  - ac: skills/ship#ac:emits-ship-completed-once
    verdict: pass
    justification: "Lifecycle Transition step 2 + events.md: emit exactly one ship.completed with publication_result; never on refusal/hand-back."
  - ac: skills/ship#ac:bars-execution-and-orchestration
    verdict: pass
    justification: "Architectural Boundary bars deploy mechanics/sequencing/retry/rollback/canary/flag-flips/scheduling/multi-feature; ship: config exposes only delegate.skill+args."
```

# Verify Report — skills/ship @ 2381566

All 11 acceptance criteria mapped to exactly one implementation commit and verified `pass`. The implementation is a markdown-driven skill (`skills/ship/SKILL.md`) plus config/doc registrations; each AC was verified by confirming its `Then`-clause predicate is satisfied by the committed text (AC-verification adapter for non-executable surfaces).

## AC: rejects-non-feature-input

**Verdict:** pass

**Justification:** Pre-Flight step 1 ("Resolve input") mandates that zero or more than one positional argument is a usage error: print usage, write nothing, exit non-zero — all three `Then` predicates.

**Commits:**
- cc4d780 — feat(ship): scaffold ship skill — input resolution + status pre-flight

**Evidence:**
- skills/ship/SKILL.md — Pre-Flight step 1; When to Use refuse/redirect bullet

## AC: refuses-non-implementing-status

**Verdict:** pass

**Justification:** Pre-Flight step 2 ("Status guard") + preamble: print the current Status, refuse unless exactly `Implementing`, dispatch no reviewer and no delegate, exit non-zero.

**Commits:**
- cc4d780 — feat(ship): scaffold ship skill — input resolution + status pre-flight

**Evidence:**
- skills/ship/SKILL.md — Pre-Flight preamble + step 2; When to Use refuse/redirect bullet

## AC: refuses-when-verify-not-green

**Verdict:** pass

**Justification:** Pre-Flight step 3 ("Verify-green gate") refuses unless every verify AC verdict is `pass` (or when the report is missing), names the condition, recommends `specstudio:verify`, exits non-zero.

**Commits:**
- 923575f — feat(ship): add verify-green and recap-no-contradiction pre-flight gates

**Evidence:**
- skills/ship/SKILL.md — Pre-Flight step 3; When to Use refuse/redirect bullet

## AC: refuses-on-recap-contradiction

**Verdict:** pass

**Justification:** Pre-Flight step 4 ("Recap-no-contradiction gate") refuses on any `contradiction` verdict or missing recap, names the condition, recommends `specstudio:recap`, exits non-zero; recap is strictly mandatory.

**Commits:**
- 923575f — feat(ship): add verify-green and recap-no-contradiction pre-flight gates

**Evidence:**
- skills/ship/SKILL.md — Pre-Flight step 4; When to Use refuse/redirect bullet

## AC: gate-releases-only-on-all-approved

**Verdict:** pass

**Justification:** Reviewer Gate section: AND-composed, releases only when every entry returns `Approved`, proceeds to dispatch on release, and halts (no delegate, no status transition) on `Issues Found`. `ship.pre_dispatch` is registered in `events.md` and acknowledged in the reviewer-gates Feature.

**Commits:**
- 6c3d18d — feat(ship): register ship.pre_dispatch gate-point + wire reviewer gate

**Evidence:**
- skills/ship/SKILL.md — Reviewer Gate steps 2–3
- skills/shared/events.md — `ship.pre_dispatch` section + catalog row
- spec/features/reviewer-gates/README.md — REQ:gate-point-events-and-multi-fire

## AC: dispatches-single-configured-delegate

**Verdict:** pass

**Justification:** Deploy Dispatch item 1 invokes the single configured delegate exactly once with `delegate.args`, with no sequencing or retry; the `ship:` schema (delegate.skill + args) is present in `specscore.yaml`.

**Commits:**
- 33ab0b3 — feat(ship): define ship: config schema and deploy dispatch

**Evidence:**
- skills/ship/SKILL.md — Deploy Dispatch (config schema + behavior item 1)
- specscore.yaml — `ship:` block

## AC: hands-back-when-no-delegate

**Verdict:** pass

**Justification:** Deploy Dispatch item 2: with no `ship.delegate`, summarize gate results, state no delegate is configured, transition no status, emit no `ship.completed`, exit without deploying.

**Commits:**
- 33ab0b3 — feat(ship): define ship: config schema and deploy dispatch

**Evidence:**
- skills/ship/SKILL.md — Deploy Dispatch behavior item 2

## AC: no-transition-on-delegate-failure

**Verdict:** pass

**Justification:** Deploy Dispatch item 3: on delegate failure or ambiguous result, leave the Feature in `Implementing`, do not retry, surface the outcome, emit no `ship.completed`.

**Commits:**
- 33ab0b3 — feat(ship): define ship: config schema and deploy dispatch

**Evidence:**
- skills/ship/SKILL.md — Deploy Dispatch behavior item 3

## AC: transitions-to-stable-on-success

**Verdict:** pass

**Justification:** Lifecycle Transition step 1 runs `specscore feature change-status <feature-slug> --to Stable` only on the delegate's explicit success.

**Commits:**
- 6020b3c — feat(ship): lifecycle transition to Stable + ship.completed event

**Evidence:**
- skills/ship/SKILL.md — Lifecycle Transition and Event, step 1

## AC: emits-ship-completed-once

**Verdict:** pass

**Justification:** Lifecycle Transition step 2 + `events.md`: apply publication policy then emit exactly one `ship.completed` with `publication_result`; never on refusal or no-delegate hand-back.

**Commits:**
- 6020b3c — feat(ship): lifecycle transition to Stable + ship.completed event

**Evidence:**
- skills/ship/SKILL.md — Lifecycle Transition and Event, step 2
- skills/shared/events.md — `ship.completed` section + invariants

## AC: bars-execution-and-orchestration

**Verdict:** pass

**Justification:** Architectural Boundary bars ship from deploy mechanics, sequencing, retry, rollback, canary, flag-flips, scheduling, and multi-feature coordination, and constrains the `ship:` config to only `delegate.skill`/`delegate.args`.

**Commits:**
- 57dcdbd — feat(ship): encode architectural boundary + add recap->ship lifecycle node

**Evidence:**
- skills/ship/SKILL.md — Architectural Boundary
- specscore.yaml — `ship:` block bounded-fields comment

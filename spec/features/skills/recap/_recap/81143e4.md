# Recap Report: skills/recap @ 81143e4

```yaml
feature: skills/recap
revision: 81143e4
verify_revision: a05f52b
drift:
  - ac: skills/recap#ac:refuses-draft-feature
    verdict: no-drift
    narrative: "Pre-Flight step 2 + When-to-Use refuse-and-redirect match AC's four predicates exactly: refuse outside {Approved, Implementing, Stable}, print Status, recommend specstudio:specify, no report, non-zero exit."
  - ac: skills/recap#ac:refuses-uncommitted-feature
    verdict: no-drift
    narrative: "Pre-Flight step 3 uses the exact mechanism named in AC (git cat-file -e HEAD:<path>) plus the exact three predicates. Mechanism and outcome match precisely."
  - ac: skills/recap#ac:refuses-when-no-verify-report
    verdict: no-drift
    narrative: "Pre-Flight step 4 names both branches of the AC disjunction (absent OR zero-reports-reachable-at-HEAD) plus the three predicates (recommend verify-first, no report, non-zero exit). Matches."
  - ac: skills/recap#ac:verify-report-resolved-to-latest-at-head
    verdict: no-drift
    narrative: "Checklist step 3 names both AC sub-cases (HEAD-SHA-exact-match preferred, fallback to most-recent revision: in branch history) and records resolved path/SHA for verify_revision: and verify_report_path. Matches."
  - ac: skills/recap#ac:per-ac-trailer-grep
    verdict: no-drift
    narrative: "Checklist step 4 names the exact command (git log --extended-regexp --grep='^Verifies:.*<feature-slug>#ac:<ac-slug>'), step 2 delegates Feature parse to the specscore CLI, unmapped ACs are skipped for dispatch. Three predicates met."
  - ac: skills/recap#ac:unmapped-not-contradiction
    verdict: no-drift
    narrative: "Step 4 marks zero-commit ACs as unmapped (orchestrator-only); Exit Semantics lists unmapped → 0; YAML example includes verdict: unmapped; Drift-Narrator Contract excludes unmapped from subagent allowed-verdict set. Four predicates met."
  - ac: skills/recap#ac:subagent-serial-dispatch
    verdict: no-drift
    narrative: "Checklist step 5 names the discipline verbatim ('one drift-narrator subagent at a time... no concurrent in-flight'). Red Flags reinforces. Exact match to AC."
  - ac: skills/recap#ac:subagent-prompt-shape
    verdict: no-drift
    narrative: "Drift-Narrator Subagent Contract numbers all five required parts in order. Contract block quoted verbatim with the four-value set only. Fetch-on-demand instruction explicit. Out-of-scope section forbids pre-fetched diffs and unmapped/error in allowed set. Mirrors AC exactly."
  - ac: skills/recap#ac:malformed-drift-verdict-retried-once
    verdict: no-drift
    narrative: "Checklist step 6 quotes the AC's retry policy verbatim: re-dispatch exactly once on malformed first, record error on second malformed, no third call. Defines all three malformed cases. Red Flags reinforces."
  - ac: skills/recap#ac:report-path-and-staging
    verdict: no-drift
    narrative: "Report Format → Path pins the exact canonical path; Staging discipline runs git add and forbids git commit; Checklist step 10 stages both files in a single git add invocation. Three predicates match exactly."
  - ac: skills/recap#ac:report-yaml-block-grep-target
    verdict: no-drift
    narrative: "YAML summary block requires fenced ```yaml first with feature/revision/verify_revision top-level + per-AC ac/verdict/narrative in Feature AC order. Body template specifies all six per-AC body elements (drift verdict, narrative, verify carry-over, commits, evidence). Matches."
  - ac: skills/recap#ac:report-index-readme-created-and-updated
    verdict: no-drift
    narrative: "Index README provides creation template matching all five required elements (H1, paragraph, 4-column table, Open Questions, footer); append rule preserves prior rows newest-last. Newest-last ordering decision documented per AC's Approach note. Matches."
  - ac: skills/recap#ac:exit-non-zero-on-contradiction-only
    verdict: no-drift
    narrative: "Exit Semantics: non-zero if and only if contradiction_count + errored_count > 0; explicit table covers all three AC sub-cases (contradiction non-zero, mixed informational zero, error non-zero). Informational-only treatment of spec-tighter/code-tighter/unmapped called out."
  - ac: skills/recap#ac:recap-completed-event-emitted
    verdict: no-drift
    narrative: "Event Emission pins all ten payload fields (4 identity + 6 flat counts) with exactly-once semantics, non-negative-integer invariant, sum-equals-AC-count invariant, no per-AC details. Red Flags reinforces flat-not-nested. Matches."
  - ac: skills/recap#ac:transition-to-review-only
    verdict: no-drift
    narrative: "Hard Gate item 7 + Promotion Boundary name the exact forbidden-skill list verbatim (ideate, specify, plan, implement, verify, ship, writing-plans, frontend-design, mcp-builder) and define the hand-back fallback prose. Matches AC exactly."
```

## Summary

15 no-drift · 0 spec-tighter-than-code · 0 code-tighter-than-spec · 0 contradiction · 0 unmapped

All 15 acceptance criteria in the `skills/recap` Feature are satisfied by commit `77e9397` without observable drift between spec and implementation. The drift-narrator subagent probed deeper than verify (which had returned 15/15 `pass`) but found no cases where the SKILL.md's mechanism, scope, or outcome diverged from what each AC named.

**Meta-observation worth recording:** when the implementation artifact is itself a SKILL.md authored directly from the Feature's ACs, the recap drift surface collapses to near-zero because the SKILL.md's prose, hard-gate items, numbered checklist steps, contracts, and red-flag list mechanically mirror each AC's `Then` clause and each REQ's mechanism. Recap's drift-detection earns its keep when commits to *behavioral* code (not skill-prose) diverge from the spec's named mechanisms — e.g., when an AC says "MUST use approach X" and the code achieves the right outcome via approach Y. For SKILL.md commits, recap functions more as a corroboration of verify than as an independent signal. This is a known limit of the MVP design (Idea: "Recap's real signal on drift will come when commits to behavioral code diverge from spec assertions").

## AC: refuses-draft-feature

**Drift verdict:** no-drift
**Narrative:** Pre-Flight step 2 + When-to-Use refuse-and-redirect match the AC's four predicates exactly: refuse outside `{Approved, Implementing, Stable}`, print current Status, recommend `specstudio:specify` to re-approve, write no report, exit non-zero. Path and predicates match the AC verbatim.
**Verify verdict (carry-over):** `pass` — "Pre-Flight step 2 + When to Use refuse-and-redirect: prints Status, recommends specstudio:specify, writes no report, exits non-zero."
**Commits:**
- `77e9397` feat(skills/recap): implement MVP AI-subagent drift narrator — Plan 1/1
**Evidence:**
- `skills/recap/SKILL.md` §Pre-Flight step 2
- `skills/recap/SKILL.md` §When to Use (Refuse and redirect)

## AC: refuses-uncommitted-feature

**Drift verdict:** no-drift
**Narrative:** AC names the exact mechanism (`git cat-file -e HEAD:spec/features/<slug>/README.md`) and three predicates (refuse with commit-first instruction, no report, non-zero exit). Pre-Flight step 3 uses the exact same command verbatim and the exact same three predicates. Mechanism and outcome match precisely.
**Verify verdict (carry-over):** `pass` — "Pre-Flight step 3 runs git cat-file -e HEAD:<path>; refuses with commit-first instruction; writes no report; exits non-zero."
**Commits:**
- `77e9397`
**Evidence:**
- `skills/recap/SKILL.md` §Pre-Flight step 3
- `skills/recap/SKILL.md` §When to Use (Refuse and redirect)

## AC: refuses-when-no-verify-report

**Drift verdict:** no-drift
**Narrative:** AC requires refusal when `_verify/` is absent OR contains zero `<sha>.md` reports reachable at HEAD, with verify-first recommendation, no report, non-zero exit. Pre-Flight step 4 names both branches of the disjunction ("directory is absent, or it exists but contains zero `<sha>.md` reports reachable at HEAD") and the three predicates. Matches.
**Verify verdict (carry-over):** `pass` — "Pre-Flight step 4 checks _verify/ exists with ≥1 reachable <sha>.md; refuses with recommend-verify-first; writes no report; exits non-zero."
**Commits:**
- `77e9397`
**Evidence:**
- `skills/recap/SKILL.md` §Pre-Flight step 4
- `skills/recap/SKILL.md` §When to Use (Refuse and redirect)

## AC: verify-report-resolved-to-latest-at-head

**Drift verdict:** no-drift
**Narrative:** Both AC sub-cases mapped: HEAD-SHA-exact-match preferred ("`<sha-C>` is the abbreviated SHA of `HEAD`"), fallback to most-recent `revision:` field in branch history. Checklist step 3 names both branches with the exact wording, and explicitly records the resolved report's path + `revision:` for `verify_revision:` and `verify_report_path`. Matches.
**Verify verdict (carry-over):** `pass` — "Checklist step 3 specifies HEAD-SHA-exact-match first, fallback to most-recent revision: field; resolved path feeds verify_revision: in report YAML."
**Commits:**
- `77e9397`
**Evidence:**
- `skills/recap/SKILL.md` §Checklist step 3
- `skills/recap/SKILL.md` §Report Format → YAML summary block

## AC: per-ac-trailer-grep

**Drift verdict:** no-drift
**Narrative:** AC requires per-AC `git log --grep='^Verifies:.*<feature-slug>#ac:<ac-slug>'` (extended regexp), with subagent dispatch only for mapped ACs. Checklist step 4 names the exact command (`git log --extended-regexp --grep='^Verifies:.*<feature-slug>#ac:<ac-slug>'`) and skips unmapped ACs for dispatch. Step 2 delegates Feature parse to the `specscore` CLI as `REQ:feature-parse` requires.
**Verify verdict (carry-over):** `pass` — "Checklist step 2 delegates Feature parse to specscore CLI; step 4 runs git log --extended-regexp --grep per AC; unmapped ACs skipped for dispatch."
**Commits:**
- `77e9397`
**Evidence:**
- `skills/recap/SKILL.md` §Checklist steps 2, 4, 5

## AC: unmapped-not-contradiction

**Drift verdict:** no-drift
**Narrative:** AC requires zero-commit AC marked `verdict: unmapped`, exit zero when other ACs are `no-drift`, and the AC listed in YAML summary. Checklist step 4 marks zero-commit ACs as `unmapped` (orchestrator-produced); Exit Semantics table explicitly lists unmapped → 0; YAML summary example includes `verdict: unmapped`. Drift-Narrator Subagent Contract excludes `unmapped` from the allowed-verdict set named to the subagent.
**Verify verdict (carry-over):** `pass` — "Zero-commit ACs recorded as unmapped (orchestrator-only); YAML example shows verdict: unmapped; Exit Semantics table lists unmapped-only → 0; unmapped excluded from subagent prompt."
**Commits:**
- `77e9397`
**Evidence:**
- `skills/recap/SKILL.md` §Checklist step 4
- `skills/recap/SKILL.md` §Exit Semantics
- `skills/recap/SKILL.md` §Drift-Narrator Subagent Contract → Out of scope

## AC: subagent-serial-dispatch

**Drift verdict:** no-drift
**Narrative:** AC requires exactly one drift-narrator subagent at a time in AC order; no concurrent in-flight. Checklist step 5 says "dispatch one drift-narrator subagent ... in Feature AC order. Wait for the subagent to return ... At no point ... MUST more than one narrator subagent be concurrently in flight." Red Flags reinforces.
**Verify verdict (carry-over):** `pass` — "Checklist step 5 + Red Flags: dispatch one drift-narrator subagent at a time in AC order; no concurrent in-flight subagents."
**Commits:**
- `77e9397`
**Evidence:**
- `skills/recap/SKILL.md` §Checklist step 5
- `skills/recap/SKILL.md` §Red Flags

## AC: subagent-prompt-shape

**Drift verdict:** no-drift
**Narrative:** AC requires a five-part prompt in order: G/W/T, verify verdict+justification verbatim, commits+messages, verbatim drift contract, fetch-on-demand instruction. Forbids pre-fetched diffs and `unmapped`/`error` in allowed set. Drift-Narrator Subagent Contract → Prompt shape numbers all five parts in the exact required order, contract block is quoted verbatim with the four-value set only, fetch-on-demand instruction is explicit, Out-of-scope section forbids pre-fetched diffs and excludes `unmapped`/`error`. Mirrors AC exactly.
**Verify verdict (carry-over):** `pass` — "Drift-Narrator Subagent Contract → Prompt shape lists all five required parts in order; explicitly forbids pre-fetched diffs; excludes unmapped/error from allowed-verdict set."
**Commits:**
- `77e9397`
**Evidence:**
- `skills/recap/SKILL.md` §Drift-Narrator Subagent Contract → Prompt shape
- `skills/recap/SKILL.md` §Drift-Narrator Subagent Contract → Out of scope

## AC: malformed-drift-verdict-retried-once

**Drift verdict:** no-drift
**Narrative:** AC requires retry exactly once on malformed first response, record `error` on second malformed, no third call. Checklist step 6 says verbatim: "re-dispatch the same subagent exactly once with a corrective prompt ... On a malformed second response, record the AC's drift verdict as `error` ... MUST NOT call the subagent a third time." Defines all three malformed cases. Red Flags reinforces.
**Verify verdict (carry-over):** `pass` — "Checklist step 6: re-dispatch exactly once on malformed first; record error on malformed second; defines malformed (verdict out of set, missing narrative, >500 chars)."
**Commits:**
- `77e9397`
**Evidence:**
- `skills/recap/SKILL.md` §Checklist step 6
- `skills/recap/SKILL.md` §Drift-Narrator Subagent Contract → Verdict parsing
- `skills/recap/SKILL.md` §Red Flags

## AC: report-path-and-staging

**Drift verdict:** no-drift
**Narrative:** AC requires report at `spec/features/<feature-slug>/_recap/<sha>.md` (abbreviated HEAD SHA), staged via `git add`, no `git commit`. Report Format → Path pins the exact path; Staging discipline runs `git add` and explicitly forbids `git commit`; Checklist step 10 stages both files via single `git add` invocation. Three predicates met exactly.
**Verify verdict (carry-over):** `pass` — "Report Format → Path pins spec/features/<slug>/_recap/<sha>.md; Staging discipline runs git add and forbids git commit; step 10 stages both report + index."
**Commits:**
- `77e9397`
**Evidence:**
- `skills/recap/SKILL.md` §Report Format → Path
- `skills/recap/SKILL.md` §Report Format → Staging discipline
- `skills/recap/SKILL.md` §Checklist step 10

## AC: report-yaml-block-grep-target

**Drift verdict:** no-drift
**Narrative:** AC requires fenced ```yaml block first with top-level `feature:`, `revision:`, `verify_revision:` plus per-AC `ac`/`verdict`/`narrative` entries in Feature AC order, plus per-AC `## AC: <ac-slug>` body sections carrying drift verdict, narrative, verify verdict, verify justification verbatim, commits, evidence refs. Report Format → YAML summary block and Body sections specify all required fields and ordering. Body shape template covers all six required body elements.
**Verify verdict (carry-over):** `pass` — "YAML summary block mandates fenced ```yaml first with feature/revision/verify_revision/drift per-AC ac/verdict/narrative; Body specifies ## AC: <slug> sections with verify carry-over."
**Commits:**
- `77e9397`
**Evidence:**
- `skills/recap/SKILL.md` §Report Format → YAML summary block
- `skills/recap/SKILL.md` §Report Format → Body

## AC: report-index-readme-created-and-updated

**Drift verdict:** no-drift
**Narrative:** AC requires absent-case create (H1, paragraph, 4-column table `Report | Run revision | Verify revision | Drift summary`, `## Open Questions = "None at this time."`, specscore index-spec footer) and present-case append (preserve prior rows, N→N+1, lint zero). Report Format → Index README provides exact creation template matching all five elements, append rule explicitly preserves prior rows newest-last. Newest-last ordering documented per the AC's Approach note.
**Verify verdict (carry-over):** `pass` — "Index README specifies absent-case creation shape (H1, table columns, Open Questions, footer) and present-case append rule (newest-last, preserve prior); step 10 stages alongside per-run report."
**Commits:**
- `77e9397`
**Evidence:**
- `skills/recap/SKILL.md` §Report Format → Index README
- `skills/recap/SKILL.md` §Checklist steps 9-10

## AC: exit-non-zero-on-contradiction-only

**Drift verdict:** no-drift
**Narrative:** AC has three sub-cases: contradiction → non-zero; all-{no-drift, spec-tighter, code-tighter, unmapped} → zero; error → non-zero. Exit Semantics: "non-zero **if and only if** `contradiction_count + errored_count > 0`" with explicit table covering all three sub-cases. Informational-only treatment of spec-tighter/code-tighter/unmapped called out explicitly.
**Verify verdict (carry-over):** `pass` — "Exit Semantics: non-zero if and only if contradiction_count + errored_count > 0; table covers all three AC sub-cases."
**Commits:**
- `77e9397`
**Evidence:**
- `skills/recap/SKILL.md` §Exit Semantics

## AC: recap-completed-event-emitted

**Drift verdict:** no-drift
**Narrative:** AC requires exactly one `recap.completed` event with four identity fields (`feature_slug`, `revision`, `report_path`, `verify_report_path`) plus six flat count fields summing to AC count, non-negative ints, no per-AC details. Event Emission section pins all ten fields by name with exactly the AC's invariants ("exactly once", "all six count fields are non-negative integers", sum invariant, "MUST NOT include per-AC drift details"). Red Flags reinforces flat-not-nested.
**Verify verdict (carry-over):** `pass` — "Event Emission pins exactly-once recap.completed with 4 identity + 6 flat count fields; invariants: non-negative ints, sum == AC count, no per-AC details, flat counts."
**Commits:**
- `77e9397`
**Evidence:**
- `skills/recap/SKILL.md` §Event Emission
- `skills/recap/SKILL.md` §Red Flags

## AC: transition-to-review-only

**Drift verdict:** no-drift
**Narrative:** AC requires offering only `specstudio:review` (or hand-back when unshipped), explicitly forbidding ideate/specify/plan/implement/verify/ship/writing-plans/frontend-design/mcp-builder. Hard Gate item 7 + Promotion Boundary section name the exact forbidden-skill list verbatim and define the hand-back fallback prose.
**Verify verdict (carry-over):** `pass` — "Hard Gate item 7 + Promotion Boundary: only specstudio:review (or hand-back); explicitly forbids ideate, specify, plan, implement, verify, ship, writing-plans, frontend-design, mcp-builder."
**Commits:**
- `77e9397`
**Evidence:**
- `skills/recap/SKILL.md` §Hard Gate
- `skills/recap/SKILL.md` §Promotion Boundary

---
*This document follows the https://specscore.md/recap-report-specification*

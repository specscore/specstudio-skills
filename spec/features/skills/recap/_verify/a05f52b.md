# Verify Report: skills/recap @ a05f52b

```yaml
feature: skills/recap
revision: a05f52b
verdicts:
  - ac: skills/recap#ac:refuses-draft-feature
    verdict: pass
    justification: "Pre-Flight step 2 + When to Use refuse-and-redirect: prints Status, recommends specstudio:specify, writes no report, exits non-zero."
  - ac: skills/recap#ac:refuses-uncommitted-feature
    verdict: pass
    justification: "Pre-Flight step 3 runs git cat-file -e HEAD:<path>; refuses with commit-first instruction; writes no report; exits non-zero."
  - ac: skills/recap#ac:refuses-when-no-verify-report
    verdict: pass
    justification: "Pre-Flight step 4 checks _verify/ exists with ≥1 reachable <sha>.md; refuses with recommend-verify-first; writes no report; exits non-zero."
  - ac: skills/recap#ac:verify-report-resolved-to-latest-at-head
    verdict: pass
    justification: "Checklist step 3 specifies HEAD-SHA-exact-match first, fallback to most-recent revision: field; resolved path feeds verify_revision: in report YAML."
  - ac: skills/recap#ac:per-ac-trailer-grep
    verdict: pass
    justification: "Checklist step 2 delegates Feature parse to specscore CLI; step 4 runs git log --extended-regexp --grep per AC; unmapped ACs skipped for dispatch."
  - ac: skills/recap#ac:unmapped-not-contradiction
    verdict: pass
    justification: "Zero-commit ACs recorded as unmapped (orchestrator-only); YAML example shows verdict: unmapped; Exit Semantics table lists unmapped-only → 0; unmapped excluded from subagent prompt."
  - ac: skills/recap#ac:subagent-serial-dispatch
    verdict: pass
    justification: "Checklist step 5 + Red Flags: dispatch one drift-narrator subagent at a time in AC order; no concurrent in-flight subagents."
  - ac: skills/recap#ac:subagent-prompt-shape
    verdict: pass
    justification: "Drift-Narrator Subagent Contract → Prompt shape lists all five required parts in order; explicitly forbids pre-fetched diffs; excludes unmapped/error from allowed-verdict set."
  - ac: skills/recap#ac:malformed-drift-verdict-retried-once
    verdict: pass
    justification: "Checklist step 6: re-dispatch exactly once on malformed first; record error on malformed second; defines malformed (verdict out of set, missing narrative, >500 chars)."
  - ac: skills/recap#ac:report-path-and-staging
    verdict: pass
    justification: "Report Format → Path pins spec/features/<slug>/_recap/<sha>.md; Staging discipline runs git add and forbids git commit; step 10 stages both report + index."
  - ac: skills/recap#ac:report-yaml-block-grep-target
    verdict: pass
    justification: "YAML summary block mandates fenced ```yaml first with feature/revision/verify_revision/drift per-AC ac/verdict/narrative; Body specifies ## AC: <slug> sections with verify carry-over."
  - ac: skills/recap#ac:report-index-readme-created-and-updated
    verdict: pass
    justification: "Index README specifies absent-case creation shape (H1, table columns, Open Questions, footer) and present-case append rule (newest-last, preserve prior); step 10 stages alongside per-run report."
  - ac: skills/recap#ac:exit-non-zero-on-contradiction-only
    verdict: pass
    justification: "Exit Semantics: non-zero if and only if contradiction_count + errored_count > 0; table covers all three AC sub-cases (contradiction, unmapped/spec-tighter/code-tighter, error)."
  - ac: skills/recap#ac:recap-completed-event-emitted
    verdict: pass
    justification: "Event Emission pins exactly-once recap.completed with 4 identity + 6 flat count fields; invariants: non-negative ints, sum == AC count, no per-AC details, flat counts."
  - ac: skills/recap#ac:transition-to-review-only
    verdict: pass
    justification: "Hard Gate item 7 + Promotion Boundary: only specstudio:review (or hand-back); explicitly forbids ideate, specify, plan, implement, verify, ship, writing-plans, frontend-design, mcp-builder."
```

## Summary

15/15 pass · 0 fail · 0 error · 0 unmapped

All 15 acceptance criteria in the `skills/recap` Feature are satisfied by commit `77e9397` (`feat(skills/recap): implement MVP AI-subagent drift narrator — Plan 1/1`). The `skills/recap/SKILL.md` document contains the prose, hard-gate items, numbered procedure steps, and explicit instructions that — were an agent to follow the skill — would cause it to satisfy every AC's `Then` clause.

The implementation deliberately mirrors `skills/verify/SKILL.md` per the Plan's `## Approach`; the seven documented divergences (third pre-flight guard for verify-report-presence, verify-report-resolution responsibility, 4-bucket drift verdict contract with 500-char narrative cap and orchestrator-only `unmapped`/`error`, 4-column index README, six count fields with `contradiction OR error` blocking, transition only to `specstudio:review`) are all applied.

## AC: refuses-draft-feature

**Verdict:** pass
**Justification:** Pre-Flight step 2 + When to Use refuse-and-redirect: prints Status, recommends `specstudio:specify`, writes no report, exits non-zero. AC's three predicates (refuse, no report, non-zero exit) all explicitly named.
**Commits:**
- `77e9397` feat(skills/recap): implement MVP AI-subagent drift narrator — Plan 1/1
**Evidence:**
- `skills/recap/SKILL.md` §When to Use (refuse-and-redirect bullet for Draft/Under Review)
- `skills/recap/SKILL.md` §Pre-Flight step 2 (Status guard)

## AC: refuses-uncommitted-feature

**Verdict:** pass
**Justification:** Pre-Flight step 3 runs `git cat-file -e HEAD:<feature-path>` and refuses with commit-first instruction; writes no report; exits non-zero. Mirrors `verify`'s precedent exactly.
**Commits:**
- `77e9397`
**Evidence:**
- `skills/recap/SKILL.md` §Pre-Flight step 3
- `skills/recap/SKILL.md` §When to Use (refuse-and-redirect bullet)

## AC: refuses-when-no-verify-report

**Verdict:** pass
**Justification:** Pre-Flight step 4 checks `_verify/` exists with ≥1 `<sha>.md` reachable at HEAD; refuses with recommendation to run `specstudio:verify` first; writes no report; exits non-zero. This is the third pre-flight guard that distinguishes recap from verify.
**Commits:**
- `77e9397`
**Evidence:**
- `skills/recap/SKILL.md` §Pre-Flight step 4
- `skills/recap/SKILL.md` §When to Use (refuse-and-redirect bullet)

## AC: verify-report-resolved-to-latest-at-head

**Verdict:** pass
**Justification:** Checklist step 3 specifies HEAD-SHA-exact-match first, then most-recent `revision:` fallback in branch history; records resolved path and SHA for `verify_revision:` field and event payload's `verify_report_path`. Both AC sub-cases (HEAD-exact and recency-fallback) covered.
**Commits:**
- `77e9397`
**Evidence:**
- `skills/recap/SKILL.md` §Checklist step 3 (verify-report resolution)
- `skills/recap/SKILL.md` §Report Format → YAML summary block (top-level `verify_revision:`)

## AC: per-ac-trailer-grep

**Verdict:** pass
**Justification:** Checklist step 2 delegates Feature parse to the `specscore` CLI; step 4 runs `git log --extended-regexp --grep='^Verifies:.*<feature-slug>#ac:<ac-slug>'` per AC; collects chronological commit SHAs + messages; unmapped ACs skipped for subagent dispatch.
**Commits:**
- `77e9397`
**Evidence:**
- `skills/recap/SKILL.md` §Checklist steps 2, 4, 5

## AC: unmapped-not-contradiction

**Verdict:** pass
**Justification:** Step 4 records zero-commit ACs as `unmapped` (orchestrator-produced); YAML example shows `verdict: unmapped`; Exit Semantics table explicitly lists "every AC `no-drift`/.../`unmapped`" → exit 0; `unmapped` excluded from any subagent prompt's allowed-verdict set.
**Commits:**
- `77e9397`
**Evidence:**
- `skills/recap/SKILL.md` §Checklist step 4 (unmapped handling)
- `skills/recap/SKILL.md` §Exit Semantics table
- `skills/recap/SKILL.md` §Drift-Narrator Subagent Contract (allowed-verdict set excludes unmapped/error)

## AC: subagent-serial-dispatch

**Verdict:** pass
**Justification:** Checklist step 5: "dispatch one drift-narrator subagent ... in Feature AC order. Wait for the subagent to return ... before dispatching the next. At no point ... MUST more than one narrator subagent be concurrently in flight." Red Flags reinforces ("Dispatching two concurrently").
**Commits:**
- `77e9397`
**Evidence:**
- `skills/recap/SKILL.md` §Checklist step 5
- `skills/recap/SKILL.md` §Red Flags

## AC: subagent-prompt-shape

**Verdict:** pass
**Justification:** Drift-Narrator Subagent Contract → Prompt shape lists all five required parts in order: (1) AC G/W/T text, (2) verify verdict + justification verbatim carry-over, (3) commit SHAs + messages, (4) verbatim drift verdict contract naming only the four subagent verdicts, (5) fetch-on-demand Bash instruction. "Out of scope" section forbids pre-fetched diffs and excludes `unmapped`/`error`.
**Commits:**
- `77e9397`
**Evidence:**
- `skills/recap/SKILL.md` §Drift-Narrator Subagent Contract → Prompt shape
- `skills/recap/SKILL.md` §Drift-Narrator Subagent Contract → Out of scope

## AC: malformed-drift-verdict-retried-once

**Verdict:** pass
**Justification:** Checklist step 6: "On a malformed first response, re-dispatch the same subagent exactly once with a corrective prompt ... On a malformed second response, record the AC's drift verdict as `error` ... MUST NOT call the subagent a third time." Defines malformed (verdict outside set, missing narrative, >500 chars). Red Flags reinforces.
**Commits:**
- `77e9397`
**Evidence:**
- `skills/recap/SKILL.md` §Checklist step 6
- `skills/recap/SKILL.md` §Drift-Narrator Subagent Contract → Verdict parsing
- `skills/recap/SKILL.md` §Red Flags

## AC: report-path-and-staging

**Verdict:** pass
**Justification:** Report Format → Path pins `spec/features/<feature-slug>/_recap/<sha>.md` where `<sha>` is abbreviated HEAD SHA; Staging discipline runs `git add` for both report + index README and explicitly forbids `git commit`. Checklist step 10 covers both stages.
**Commits:**
- `77e9397`
**Evidence:**
- `skills/recap/SKILL.md` §Report Format → Path
- `skills/recap/SKILL.md` §Report Format → Staging discipline
- `skills/recap/SKILL.md` §Checklist step 10

## AC: report-yaml-block-grep-target

**Verdict:** pass
**Justification:** Report Format → YAML summary block mandates fenced ` ```yaml ` block first with `feature:`, `revision:`, `verify_revision:`, and per-AC `drift:` list with `ac`/`verdict`/`narrative` fields in Feature AC order. Body section requires one `## AC: <ac-slug>` per AC with drift verdict, narrative, verify verdict + justification verbatim, commits, evidence refs.
**Commits:**
- `77e9397`
**Evidence:**
- `skills/recap/SKILL.md` §Report Format → YAML summary block
- `skills/recap/SKILL.md` §Report Format → Body

## AC: report-index-readme-created-and-updated

**Verdict:** pass
**Justification:** Report Format → Index README specifies absent-case creation shape (H1, paragraph, `## Contents` 4-column table `Report | Run revision | Verify revision | Drift summary`, `## Open Questions` = "None at this time.", specscore index-spec footer) AND present-case append rule (newest-last, preserve prior rows). Step 10 stages alongside per-run report. Lint-zero implicit in shape compliance.
**Commits:**
- `77e9397`
**Evidence:**
- `skills/recap/SKILL.md` §Report Format → Index README
- `skills/recap/SKILL.md` §Checklist steps 9-10

## AC: exit-non-zero-on-contradiction-only

**Verdict:** pass
**Justification:** Exit Semantics: "non-zero **if and only if** `contradiction_count + errored_count > 0`". Enumeration table covers all three AC sub-cases: contradiction → non-zero; all-{no-drift,spec-tighter,code-tighter,unmapped} → zero; error → non-zero. Other drift verdicts are explicitly informational.
**Commits:**
- `77e9397`
**Evidence:**
- `skills/recap/SKILL.md` §Exit Semantics

## AC: recap-completed-event-emitted

**Verdict:** pass
**Justification:** Event Emission: "exactly one `recap.completed` event"; payload shape lists 4 identity fields (`feature_slug`, `revision`, `report_path`, `verify_report_path`) + 6 flat count fields (`no_drift_count`, `spec_tighter_count`, `code_tighter_count`, `contradiction_count`, `unmapped_count`, `errored_count`); invariants pin non-negative ints, sum == AC count, no per-AC details, flat counts (Red Flags reinforces against nested).
**Commits:**
- `77e9397`
**Evidence:**
- `skills/recap/SKILL.md` §Event Emission
- `skills/recap/SKILL.md` §Red Flags (flat counts)

## AC: transition-to-review-only

**Verdict:** pass
**Justification:** Hard Gate item 7 + Promotion Boundary: only `specstudio:review` is permitted (or hand-back when unshipped). Explicitly forbids invoking `ideate`, `specify`, `plan`, `implement`, `verify`, `ship`, `writing-plans`, `frontend-design`, `mcp-builder` on transition. AC's forbidden-skill list matches the SKILL.md's verbatim.
**Commits:**
- `77e9397`
**Evidence:**
- `skills/recap/SKILL.md` §Hard Gate
- `skills/recap/SKILL.md` §Promotion Boundary

---
*This document follows the https://specscore.md/verify-report-specification*

```yaml
feature: cli-detection-convention
revision: 55eb445
verify_revision: 55eb445
drift:
  - ac: cli-detection-convention#ac:doc-exists
    verdict: no-drift
    narrative: "cli-detection.md describes call-and-branch on exit status; response table has Optional/Mandatory/Capability-gated/Wizard rows — matches the AC."
  - ac: cli-detection-convention#ac:no-command-v
    verdict: no-drift
    narrative: "All three converted skills detect by invoking specscore and branching on exit status; grep for 'command -v specscore' returns zero matches."
  - ac: cli-detection-convention#ac:four-outcomes
    verdict: no-drift
    narrative: "The 'one mechanism' table enumerates exactly success / 127 / dedicated too-old code / other non-zero, each with its meaning."
  - ac: cli-detection-convention#ac:optional-127-fallback
    verdict: no-drift
    narrative: "ideate Step 3a maps exit 127 to the fallback path (Step 3c — direct write), exactly as the AC requires."
  - ac: cli-detection-convention#ac:optional-nonzero-no-fallback
    verdict: no-drift
    narrative: "ideate Step 3a maps any non-127 non-zero exit to 'surface the error and do NOT fall back'; only 127 takes the fallback."
  - ac: cli-detection-convention#ac:mandatory-absent-install
    verdict: no-drift
    narrative: "relocate-idea Pre-flight: on the relocate call's 127 exit, emit the verbatim /specscore:install message and stop without writing."
  - ac: cli-detection-convention#ac:capability-upgrade
    verdict: no-drift
    narrative: "consilium Pre-flight routes the dedicated too-old/missing-subcommand code to a distinct upgrade message, separate from 127 and generic failure."
  - ac: cli-detection-convention#ac:capability-precheck-order
    verdict: no-drift
    narrative: "consilium states the capability check MUST run in Pre-flight, before the Stage 3 nine-role panel, never after."
  - ac: cli-detection-convention#ac:ideate-cites
    verdict: no-drift
    narrative: "ideate Step 3a removes the command -v probe and cites ../shared/cli-detection.md."
  - ac: cli-detection-convention#ac:relocate-cites
    verdict: no-drift
    narrative: "relocate-idea Pre-flight marks itself mandatory-class, cites ../shared/cli-detection.md, and applies the full 127/other-non-zero policy."
  - ac: cli-detection-convention#ac:consilium-cites
    verdict: no-drift
    narrative: "consilium Pre-flight cites ../shared/cli-detection.md and applies the capability-gated policy (127 / too-old / other non-zero)."
```

# Recap Report — cli-detection-convention @ 55eb445

Per-AC spec-vs-code drift, compared against verify report `_verify/55eb445.md`. Drift verdicts are the drift-narrator subagents' as returned; re-judgment is `review`'s job.

**Summary:** 11 no-drift, 0 spec-tighter, 0 code-tighter, 0 contradiction, 0 unmapped, 0 errored (11 ACs total).

## AC: doc-exists

**Drift verdict:** no-drift

**Narrative:** `skills/shared/cli-detection.md` describes the call-and-branch detection on exit status, and the response table carries Optional/Mandatory/Capability-gated/Wizard rows — exactly what the AC requires.

**Verify verdict:** pass

**Verify justification:** cli-detection.md describes call-and-branch on exit status; response table has Optional/Mandatory/Capability-gated/Wizard rows.

**Commits:**
- 55eb445 — feat(skills): adopt one CLI-detection mechanism with per-class response

**Evidence:**
- skills/shared/cli-detection.md ("The one mechanism" + "The response table")

## AC: no-command-v

**Drift verdict:** no-drift

**Narrative:** All three converted skills detect by invoking `specscore` and branching on exit status; each explicitly forbids a standalone `command -v` probe, and grep for `command -v specscore` returns zero matches.

**Verify verdict:** pass

**Verify justification:** All three skills detect by invoking specscore and branching on exit status; each explicitly forbids a standalone command -v probe.

**Commits:**
- 55eb445 — feat(skills): adopt one CLI-detection mechanism with per-class response

**Evidence:**
- skills/ideate/SKILL.md (Step 3a), skills/relocate-idea/SKILL.md (Pre-flight), skills/consilium/SKILL.md (Pre-flight)

## AC: four-outcomes

**Drift verdict:** no-drift

**Narrative:** The "one mechanism" table enumerates exactly success (0), 127, the dedicated too-old/missing-subcommand code, and any other non-zero, each with its meaning.

**Verify verdict:** pass

**Verify justification:** "The one mechanism" table enumerates success / 127 / dedicated too-old code / other non-zero, each with its meaning.

**Commits:**
- 55eb445 — feat(skills): adopt one CLI-detection mechanism with per-class response

**Evidence:**
- skills/shared/cli-detection.md ("The one mechanism" four-row table)

## AC: optional-127-fallback

**Drift verdict:** no-drift

**Narrative:** ideate Step 3a maps exit `127` to the fallback path (Step 3c — direct write), exactly as the AC requires; detection is via the shared attempt-then-branch mechanism, not a standalone probe.

**Verify verdict:** pass

**Verify justification:** ideate Step 3a: exit 127 -> take the fallback path (Step 3c).

**Commits:**
- 55eb445 — feat(skills): adopt one CLI-detection mechanism with per-class response

**Evidence:**
- skills/ideate/SKILL.md (Step 3a → Step 3c direct write)

## AC: optional-nonzero-no-fallback

**Drift verdict:** no-drift

**Narrative:** ideate Step 3a maps any non-127 non-zero exit of `specscore idea new` to "surface the error and do NOT fall back," exactly matching the AC; only 127 takes the fallback.

**Verify verdict:** pass

**Verify justification:** ideate Step 3a: any other non-zero -> surface the error and do NOT fall back.

**Commits:**
- 55eb445 — feat(skills): adopt one CLI-detection mechanism with per-class response

**Evidence:**
- skills/ideate/SKILL.md (Step 3a)

## AC: mandatory-absent-install

**Drift verdict:** no-drift

**Narrative:** relocate-idea Pre-flight detects absence via the relocate call's `127` exit, emits the verbatim standardized `/specscore:install` message, and stops without writing — exactly matching the AC.

**Verify verdict:** pass

**Verify justification:** relocate-idea Pre-flight: on 127 emit the standardized /specscore:install message and stop without writing.

**Commits:**
- 55eb445 — feat(skills): adopt one CLI-detection mechanism with per-class response

**Evidence:**
- skills/relocate-idea/SKILL.md (Pre-flight step 1)

## AC: capability-upgrade

**Drift verdict:** no-drift

**Narrative:** consilium Pre-flight routes the dedicated "unsupported subcommand / too old" exit code to a distinct upgrade message naming the missing subcommand, separate from 127 (install) and other non-zero (generic CLI error).

**Verify verdict:** pass

**Verify justification:** consilium Pre-flight: dedicated unsupported-subcommand/too-old code -> upgrade message, distinct from 127 and generic failure.

**Commits:**
- 55eb445 — feat(skills): adopt one CLI-detection mechanism with per-class response

**Evidence:**
- skills/consilium/SKILL.md (Pre-flight step 1); skills/shared/cli-detection.md (response table)

## AC: capability-precheck-order

**Drift verdict:** no-drift

**Narrative:** consilium states the capability check MUST run in Pre-flight, before the Stage 3 nine-role panel, never after — exactly what the AC requires.

**Verify verdict:** pass

**Verify justification:** consilium states the capability check runs before the Stage 3 nine-role panel, never after.

**Commits:**
- 55eb445 — feat(skills): adopt one CLI-detection mechanism with per-class response

**Evidence:**
- skills/consilium/SKILL.md (Pre-flight header + step 1; pipeline lists Expert panel as Stage 3)

## AC: ideate-cites

**Drift verdict:** no-drift

**Narrative:** ideate Step 3a removes the `command -v` probe (only a prohibition remains) and cites `../shared/cli-detection.md`.

**Verify verdict:** pass

**Verify justification:** ideate Step 3a removes the command -v probe and cites ../shared/cli-detection.md.

**Commits:**
- 55eb445 — feat(skills): adopt one CLI-detection mechanism with per-class response

**Evidence:**
- skills/ideate/SKILL.md (Step 3a)

## AC: relocate-cites

**Drift verdict:** no-drift

**Narrative:** relocate-idea Pre-flight marks itself mandatory-class, cites `../shared/cli-detection.md`, and applies the full policy (127 → verbatim install message → stop; other non-zero → surface error → stop).

**Verify verdict:** pass

**Verify justification:** relocate-idea Pre-flight follows the mandatory-response policy and cites ../shared/cli-detection.md.

**Commits:**
- 55eb445 — feat(skills): adopt one CLI-detection mechanism with per-class response

**Evidence:**
- skills/relocate-idea/SKILL.md (Pre-flight step 1)

## AC: consilium-cites

**Drift verdict:** no-drift

**Narrative:** consilium Pre-flight cites `../shared/cli-detection.md` and applies the capability-gated policy (127 → install/stop; too-old code → upgrade; other non-zero → surface error).

**Verify verdict:** pass

**Verify justification:** consilium Pre-flight follows the capability-gated-response policy and cites ../shared/cli-detection.md.

**Commits:**
- 55eb445 — feat(skills): adopt one CLI-detection mechanism with per-class response

**Evidence:**
- skills/consilium/SKILL.md (Pre-flight step 1)

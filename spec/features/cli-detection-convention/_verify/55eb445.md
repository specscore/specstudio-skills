```yaml
feature: cli-detection-convention
revision: 55eb445
verdicts:
  - ac: cli-detection-convention#ac:doc-exists
    verdict: pass
    justification: "cli-detection.md describes call-and-branch on exit status; response table has Optional/Mandatory/Capability-gated/Wizard rows."
  - ac: cli-detection-convention#ac:no-command-v
    verdict: pass
    justification: "All three skills detect by invoking specscore and branching on exit status; each explicitly forbids a standalone command -v probe."
  - ac: cli-detection-convention#ac:four-outcomes
    verdict: pass
    justification: "'The one mechanism' table enumerates success / 127 / dedicated too-old code / other non-zero, each with its meaning."
  - ac: cli-detection-convention#ac:optional-127-fallback
    verdict: pass
    justification: "ideate Step 3a: exit 127 -> take the fallback path (Step 3c)."
  - ac: cli-detection-convention#ac:optional-nonzero-no-fallback
    verdict: pass
    justification: "ideate Step 3a: any other non-zero -> surface the error and do NOT fall back."
  - ac: cli-detection-convention#ac:mandatory-absent-install
    verdict: pass
    justification: "relocate-idea Pre-flight: on 127 emit the standardized /specscore:install message and stop without writing."
  - ac: cli-detection-convention#ac:capability-upgrade
    verdict: pass
    justification: "consilium Pre-flight: dedicated unsupported-subcommand/too-old code -> upgrade message, distinct from 127 and generic failure."
  - ac: cli-detection-convention#ac:capability-precheck-order
    verdict: pass
    justification: "consilium states the capability check runs before the Stage 3 nine-role panel, never after."
  - ac: cli-detection-convention#ac:ideate-cites
    verdict: pass
    justification: "ideate Step 3a removes the command -v probe and cites ../shared/cli-detection.md."
  - ac: cli-detection-convention#ac:relocate-cites
    verdict: pass
    justification: "relocate-idea Pre-flight follows the mandatory-response policy and cites ../shared/cli-detection.md."
  - ac: cli-detection-convention#ac:consilium-cites
    verdict: pass
    justification: "consilium Pre-flight follows the capability-gated-response policy and cites ../shared/cli-detection.md."
```

# Verify Report — cli-detection-convention @ 55eb445

Per-AC verdicts from `specstudio:verify`. Verdicts are the verifier subagents' as returned; re-judgment is `recap`'s job.

**Summary:** 11 passed, 0 failed, 0 unmapped, 0 errored (11 ACs total).

## AC: doc-exists

**Verdict:** pass

**Justification:** `skills/shared/cli-detection.md` ("The one mechanism") describes call-and-branch on exit status, and the response table carries rows for the Optional, Mandatory, Capability-gated, and Wizard classes.

**Commits:**
- 55eb445 — feat(skills): adopt one CLI-detection mechanism with per-class response

**Evidence:**
- skills/shared/cli-detection.md ("The one mechanism" + "The response table")

## AC: no-command-v

**Verdict:** pass

**Justification:** All three converted skills detect by invoking `specscore` and branching on exit status; each explicitly forbids a standalone `command -v` probe, and `grep "command -v specscore"` returns no matches.

**Commits:**
- 55eb445 — feat(skills): adopt one CLI-detection mechanism with per-class response

**Evidence:**
- skills/ideate/SKILL.md (Step 3a), skills/relocate-idea/SKILL.md (Pre-flight), skills/consilium/SKILL.md (Pre-flight)

## AC: four-outcomes

**Verdict:** pass

**Justification:** The "one mechanism" table enumerates exactly four outcomes — success (exit 0), 127 (not on PATH), the dedicated too-old/missing-subcommand code, and any other non-zero — each with its meaning.

**Commits:**
- 55eb445 — feat(skills): adopt one CLI-detection mechanism with per-class response

**Evidence:**
- skills/shared/cli-detection.md ("The one mechanism" four-row table)

## AC: optional-127-fallback

**Verdict:** pass

**Justification:** ideate Step 3a: "exit `127` (binary not installed) → take the fallback path (Step 3c)."

**Commits:**
- 55eb445 — feat(skills): adopt one CLI-detection mechanism with per-class response

**Evidence:**
- skills/ideate/SKILL.md (Step 3a)

## AC: optional-nonzero-no-fallback

**Verdict:** pass

**Justification:** ideate Step 3a: "any other non-zero → surface the error and do NOT fall back"; only 127 takes the fallback.

**Commits:**
- 55eb445 — feat(skills): adopt one CLI-detection mechanism with per-class response

**Evidence:**
- skills/ideate/SKILL.md (Step 3a)

## AC: mandatory-absent-install

**Verdict:** pass

**Justification:** relocate-idea Pre-flight detects 127 via the relocate call's exit status, emits the standardized `/specscore:install` message verbatim, and stops without writing.

**Commits:**
- 55eb445 — feat(skills): adopt one CLI-detection mechanism with per-class response

**Evidence:**
- skills/relocate-idea/SKILL.md (Pre-flight step 1)

## AC: capability-upgrade

**Verdict:** pass

**Justification:** consilium Pre-flight branches on the dedicated "unsupported subcommand / too old" exit code to an upgrade message, distinct from 127 (install) and any-other-non-zero (generic CLI error).

**Commits:**
- 55eb445 — feat(skills): adopt one CLI-detection mechanism with per-class response

**Evidence:**
- skills/consilium/SKILL.md (Pre-flight step 1)

## AC: capability-precheck-order

**Verdict:** pass

**Justification:** consilium states "before the Stage 3 nine-role panel" and "This capability check MUST run here, before the Stage 3 panel — never after."

**Commits:**
- 55eb445 — feat(skills): adopt one CLI-detection mechanism with per-class response

**Evidence:**
- skills/consilium/SKILL.md (Pre-flight header + step 1)

## AC: ideate-cites

**Verdict:** pass

**Justification:** ideate Step 3a removes the `command -v` probe (only a prohibition remains) and cites `../shared/cli-detection.md`, which exists.

**Commits:**
- 55eb445 — feat(skills): adopt one CLI-detection mechanism with per-class response

**Evidence:**
- skills/ideate/SKILL.md (Step 3a); skills/shared/cli-detection.md (cited file exists)

## AC: relocate-cites

**Verdict:** pass

**Justification:** relocate-idea Pre-flight follows the mandatory-response policy (127 → install message → stop; other non-zero → surface error) and cites `../shared/cli-detection.md`.

**Commits:**
- 55eb445 — feat(skills): adopt one CLI-detection mechanism with per-class response

**Evidence:**
- skills/relocate-idea/SKILL.md (Pre-flight step 1)

## AC: consilium-cites

**Verdict:** pass

**Justification:** consilium Pre-flight follows the capability-gated-response policy (127 → install/stop; too-old code → upgrade; other non-zero → surface error) and cites `../shared/cli-detection.md`.

**Commits:**
- 55eb445 — feat(skills): adopt one CLI-detection mechanism with per-class response

**Evidence:**
- skills/consilium/SKILL.md (Pre-flight step 1)

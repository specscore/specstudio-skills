```yaml
feature: autopilot
revision: d070c87
verdicts:
  - ac: autopilot#ac:enters-at-furthest-artifact
    verdict: pass
    justification: "Entry-point table + resolution step 4 cover Approved-Feature→plan, raw-prompt→ideate, empty-ask→refuse."
  - ac: autopilot#ac:chains-producers-in-order
    verdict: pass
    justification: "The Run step 3 drives ideate→specify→plan→implement→pull-request, advancing only on success; Hard Gate bars reimplementation."
  - ac: autopilot#ac:discloses-before-running
    verdict: pass
    justification: "Disclosure section emits one pre-stage message naming entry point, stages, masking-except-checkpoint, decide-and-record, and the ceiling."
  - ac: autopilot#ac:hands-back-pr-and-log
    verdict: pass
    justification: "Handback lists artifact paths, commit SHAs, PR URL, aggregated decisions log; no auto verify/ship."
  - ac: autopilot#ac:masks-human-entries-run-scoped
    verdict: pass
    justification: "Runner Step 1.6 masks type:human under active signal (neither dispatched nor counted; releases on rest), same semantics as a non-matching when:."
  - ac: autopilot#ac:quality-gates-still-block
    verdict: pass
    justification: "Step 1.6 masks only type:human; ai/deterministic/auto-approve still dispatch and block on Issues Found; conflict permanently in stop_on."
  - ac: autopilot#ac:auto-decides-without-asking
    verdict: pass
    justification: "specify Autonomous Mode: at approach-proposal, no AskUserQuestion; picks recommended approach; records to ## Autonomous Decisions."
  - ac: autopilot#ac:records-decisions-in-artifact
    verdict: pass
    justification: "Idea decisions → ## Key Assumptions; Feature/Plan → lint-clean ## Autonomous Decisions bullets; omit-when-empty enforced."
  - ac: autopilot#ac:pauses-once-at-idea
    verdict: pass
    justification: "Idea Checkpoint: default-true pauses once before specify; confirm_idea:false → no pause; already-Approved-Idea entry → no checkpoint."
  - ac: autopilot#ac:ceiling-is-one-pr
    verdict: pass
    justification: "Publish Ceiling: commits via implementation.pre_commit, pull-request invoked exactly once; bars merge/deploy/ship/second-PR/retry."
  - ac: autopilot#ac:armed-once-for-run
    verdict: pass
    justification: "Single Arming: armed once for whole run, no per-stage re-arm; implement anomaly still requires approval-autonomy continue re-arm."
  - ac: autopilot#ac:knobs-under-autonomy-autopilot
    verdict: pass
    justification: "Defaults-when-absent: publish_ceiling=pr, confirm_idea=true, stop_on=[conflict] non-removable; run-scope signal on ladder, evaporates at run end."
```

# Verify Report — autopilot @ d070c87

**Summary:** 12 passed, 0 failed, 0 unmapped, 0 errored (12 ACs total). Every acceptance criterion is satisfied by the committed implementation.

## AC: enters-at-furthest-artifact

**Verdict:** pass

**Justification:** The Entry-Point Detection table maps "Approved Feature, no Plan → plan → implement → pull-request" and "Raw prompt, no artifact → ideate → …"; resolution step 4 plus the "Refuse only" clause refuse an empty ask (no prompt, no resolvable artifact).

**Commits:**
- f7681be — feat(autopilot): orchestrator skill core (entry detection, chaining, arming)

**Evidence:**
- skills/autopilot/SKILL.md @ f7681be — Entry-Point Detection table; resolution step 4; When-to-Use "Refuse only" clause

## AC: chains-producers-in-order

**Verdict:** pass

**Justification:** "The Run" step 3 drives ideate → specify → plan → implement → pull-request in order, advancing only after each stage completes successfully; the Hard Gate bars re-implementing any producer's logic.

**Commits:**
- f7681be — feat(autopilot): orchestrator skill core (entry detection, chaining, arming)

**Evidence:**
- skills/autopilot/SKILL.md @ f7681be — "The Run" step 3; Hard Gate first bullet

## AC: discloses-before-running

**Verdict:** pass

**Justification:** The Disclosure section emits one message before any stage runs, naming the resolved entry point, stages, gate-masking (except the confirm_idea checkpoint), decide-and-record, and the local-commit + one-PR ceiling.

**Commits:**
- d070c87 — feat(autopilot): Idea checkpoint, publish ceiling, disclosure + handback

**Evidence:**
- skills/autopilot/SKILL.md @ d070c87 — "## Disclosure"; "## The Run" step 2

## AC: hands-back-pr-and-log

**Verdict:** pass

**Justification:** The Handback section lists the created artifact paths, local commit SHAs, opened PR URL, and aggregated Autonomous Decisions log, and states no auto-invocation of verify/ship (reinforced by the Hard Gate).

**Commits:**
- d070c87 — feat(autopilot): Idea checkpoint, publish ceiling, disclosure + handback

**Evidence:**
- skills/autopilot/SKILL.md @ d070c87 — "## Handback"; Hard Gate

## AC: masks-human-entries-run-scoped

**Verdict:** pass

**Justification:** Runner Step 1.6 masks every type:human entry under an active run-scoped signal — neither dispatched nor counted, gate releases on remaining entries — "exactly as a non-matching when: masks it in Step 1.5."

**Commits:**
- 1408d0c — feat(autopilot): run-scoped gate mask + decide-and-record in producers

**Evidence:**
- skills/shared/reviewer-gates/runner.md @ 1408d0c — "Step 1.6 — Apply the run-scoped autonomy mask"; runner-input table row

## AC: quality-gates-still-block

**Verdict:** pass

**Justification:** Step 1.6 masks only type:human; type:ai/deterministic/auto-approve still dispatch, contribute to the grade, and block on Issues Found/non-zero. The autonomy doc confirms the signal never masks reviewers, lint, or conflict; conflict is permanently in stop_on.

**Commits:**
- 1408d0c — feat(autopilot): run-scoped gate mask + decide-and-record in producers

**Evidence:**
- skills/shared/reviewer-gates/runner.md @ 1408d0c — Step 1.6 mask boundary
- skills/shared/autonomy-autopilot.md @ 1408d0c — "Quality gates always run"; conflict non-removable

## AC: auto-decides-without-asking

**Verdict:** pass

**Justification:** specify's "Autonomous Mode (decide-and-record)" states the approach-proposal step is not asked via AskUserQuestion; it picks the recommended approach without prompting and records the choice to a ## Autonomous Decisions bullet.

**Commits:**
- 1408d0c — feat(autopilot): run-scoped gate mask + decide-and-record in producers

**Evidence:**
- skills/specify/SKILL.md @ 1408d0c — "## Autonomous Mode (decide-and-record)"
- skills/shared/autonomy-autopilot.md @ 1408d0c — decide-and-record table

## AC: records-decisions-in-artifact

**Verdict:** pass

**Justification:** The Autonomous Decisions contract places Idea-stage decisions in the Idea's ## Key Assumptions and Feature/Plan-stage decisions as bullets in a lint-clean ## Autonomous Decisions H2, and mandates omit-when-empty (no section when no decision was auto-made).

**Commits:**
- 1408d0c — feat(autopilot): run-scoped gate mask + decide-and-record in producers

**Evidence:**
- skills/shared/autonomy-autopilot.md @ 1408d0c — "## The `## Autonomous Decisions` section" (placement, lint-clean, omit-when-empty)

## AC: pauses-once-at-idea

**Verdict:** pass

**Justification:** The Idea Checkpoint section satisfies all three clauses: default (unset/true) pauses exactly once for approval before specify as the only human stop; confirm_idea:false → no pause; entry at an already-Approved Idea → no checkpoint.

**Commits:**
- d070c87 — feat(autopilot): Idea checkpoint, publish ceiling, disclosure + handback

**Evidence:**
- skills/autopilot/SKILL.md @ d070c87 — "## Idea Checkpoint (`confirm_idea`)" (Default on / Opt-out / Not applicable when the Idea pre-exists)

## AC: ceiling-is-one-pr

**Verdict:** pass

**Justification:** The Publish Ceiling section lands commits via implementation.pre_commit, then invokes specstudio:pull-request exactly once for a single PR, and bars merge/deploy/ship/second-PR/delegate-retry (reinforced by the Hard Gate).

**Commits:**
- d070c87 — feat(autopilot): Idea checkpoint, publish ceiling, disclosure + handback

**Evidence:**
- skills/autopilot/SKILL.md @ d070c87 — "## Publish Ceiling"; Hard Gate

## AC: armed-once-for-run

**Verdict:** pass

**Justification:** The Single Arming section arms the signal once for the whole run with no per-stage re-arm; an implement-stage anomaly still requires approval-autonomy's explicit `continue` re-arm before implementation resumes.

**Commits:**
- f7681be — feat(autopilot): orchestrator skill core (entry detection, chaining, arming)

**Evidence:**
- skills/autopilot/SKILL.md @ f7681be — "## Single Arming"

## AC: knobs-under-autonomy-autopilot

**Verdict:** pass

**Justification:** The defaults-when-absent table resolves publish_ceiling=pr, confirm_idea=true, stop_on=[conflict] (non-removable) when no autonomy.autopilot block exists; the verbal trigger sets the signal at run scope on the publication-policy scope ladder and it evaporates at run end.

**Commits:**
- f6a6e3a — feat(autopilot): config namespace and run-scoped autonomy signal

**Evidence:**
- skills/shared/autonomy-autopilot.md @ f6a6e3a — "Fields and defaults-when-absent" table; "The run-scoped autonomy signal" (run scope, evaporates at run end)
